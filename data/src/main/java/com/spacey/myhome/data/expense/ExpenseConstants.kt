package com.spacey.myhome.data.expense

object ExpenseConstants {
    object Network {
        const val EXPENSES = "expenses"

        const val DATE = "date"

        const val FIELD_ID = "field_id"
        const val NAME = "name"
        const val type = "type"
        const val VALUE = "value"
    }

    object DB {
        const val TABLE = "DATE_EXPENSE"

        const val FIELD_ID = "field_id"
        const val DATE = "date"
    }
}